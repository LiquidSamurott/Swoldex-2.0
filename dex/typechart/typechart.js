function load() {
    let main = document.getElementById("main");
    let header = document.createElement("h1");
    header.innerHTML = "Type Chart";
    main.appendChild(header);

}

function generateTypeChart(container){
    const types =[
        "Air", "Ancient", "Brawler", "Bug", "Dark", "Earth", "Toxic"
    ]
}